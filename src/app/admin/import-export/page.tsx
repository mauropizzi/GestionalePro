"use client"; // Necessario per i componenti client-side

import React from "react";

export default function ImportExportPage() {
  return (
    <div style={{ padding: '2rem', textAlign: 'center' }}>
      <h1>Pagina di Importazione/Esportazione (Test)</h1>
      <p>Se vedi questa pagina, il routing per /admin/import-export funziona.</p>
    </div>
  );
}